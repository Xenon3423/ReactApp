import React from "react"
import preLoader from "./../../../assets/image/preloader.gif";

let Preloader = (props) => {
    return <div>
        <img src={preLoader} />
    </div>
}

export default Preloader;